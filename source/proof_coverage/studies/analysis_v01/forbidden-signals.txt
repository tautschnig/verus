# file<TAB>forbidden fixed substring
recursion	induction:{recursion::even_p,recursion::odd_p}
recursion	guarded_induction_certificate
# the forall checked export must reach the requirements, so they are not findings
models	finding goal-unused-precondition artifact=models::all_positive#req[0]
models	finding goal-unused-precondition artifact=models::all_positive#req[1]
# the live mutations are in the postcondition slice, not findings
mutation	finding goal-disconnected-code artifact=mutation::accumulate#assign.src4
mutation	finding goal-disconnected-code artifact=mutation::accumulate#assign.src5
# the resolved call must not be attributed to an AIR-spelled callee. Raw VIR
# paths (`traits_modular::impl&%1::step`) are the record's identities and are
# expected; AIR identifiers (`traits_modular!impl&%1.step.`) are transport
# spellings and must never reach a record field. `!` occurs in no VIR path.
traits_modular	traits_modular!
